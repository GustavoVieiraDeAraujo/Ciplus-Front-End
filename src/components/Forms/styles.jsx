import styled from 'styled-components';

export const StyleForm = styled.form`
    width: 100%;
    display: flex;
    height: max-content;
    flex-direction: column;

    div{
        display: flex;
        flex-direction: column;
    }
`